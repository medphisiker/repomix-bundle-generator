# Repomix Bundle Regenerator

Скрипт для автоматической перегенерации бандлов [Repomix](https://repomix.com/) в формате Markdown.

📖 **Официальная документация Repomix**: [https://repomix.com/guide/](https://repomix.com/guide/)

## Описание

Этот скрипт позволяет автоматически перегенерировать все бандлы, определенные в конфигурационном файле `.repomix/bundles.json`, создавая для каждого бандла отдельный файл в формате Markdown.

## Требования

1. **Python 3.6+** - скрипт использует стандартную библиотеку Python
2. **Repomix** - должен быть установлен глобально. Следуйте официальной инструкции по установке:
   [https://repomix.com/guide/installation](https://repomix.com/guide/installation)

## Установка

### Способ 1: Как Git Submodule (рекомендуется)

📁 **Структура итогового решения при подключении как git submodule**:

```
ваш_проект/
├── .git/
├── .gitmodules          # Конфигурация submodule
├── utils/
│   └── repomix-bundle-generator/  # Git submodule
│       ├── repomix-regenerate_bundles.py  # Основной инструмент
│       └── README.md                      # Документация
└── ...                  # Остальные файлы вашего проекта
```

1. Добавьте репозиторий как git submodule в ваш проект:
   ```bash
   git submodule add git@github.com:medphisiker/repomix-bundle-generator.git utils/repomix-bundle-generator
   git submodule update --init --recursive
   ```

2. Инструмент будет доступен по пути: `utils/repomix-bundle-generator/repomix-regenerate_bundles.py`

### Отключение submodule

Если вам нужно удалить submodule из проекта:

```bash
# Удалить запись о submodule из .gitmodules
git submodule deinit -f utils/repomix-bundle-generator
git rm -f utils/repomix-bundle-generator
rm -rf .git/modules/utils/repomix-bundle-generator

# Удалить кэш submodule (опционально)
git rm --cached utils/repomix-bundle-generator
```

Если нужно закоммитить изменения
```
git commit -m "Remove repomix-bundle-generator submodule"
```

### Способ 2: Ручная установка

1. Скачайте файл [`repomix-regenerate_bundles.py`](repomix-regenerate_bundles.py) в корневую директорию вашего проекта
2. Убедитесь, что у вас есть файл конфигурации `.repomix/bundles.json`

## Использование

Запустите скрипт из корневой директории проекта:
```bash
python utils/repomix-bundle-generator/repomix-regenerate_bundles.py
```

## Что делает скрипт

1. Проверяет наличие `repomix` в системе
2. Читает конфигурацию из `.repomix/bundles.json`
3. Для каждого бандла:
   - Проверяет существование файлов/директорий
   - Создает временный файл со списком файлов
   - Запускает `repomix --stdin --style markdown` для генерации выходного файла
4. Генерирует файлы вида `repomix-<имя_бандла>-output.md`

## Формат выходных файлов

Скрипт генерирует файлы в формате Markdown с расширением `.md`, например:
- `repomix-project-output.md`
- `repomix-solution-output.md`
- `repomix-docs-output.md`

## Обработка ошибок

Скрипт обрабатывает следующие ситуации:
- Отсутствие repomix в системе
- Отсутствие файла конфигурации `.repomix/bundles.json`
- Ошибки при чтении JSON
- Отсутствие файлов/директорий, указанных в конфигурации

## Пример конфигурации

Скрипт работает с конфигурацией вида:
```json
{
  "bundles": {
    "project-722": {
      "name": "project",
      "files": [
        "description.md",
        "pyproject.toml",
        "README.md"
      ]
    },
    "solution-484": {
      "name": "solution",
      "files": [
        "solution.py"
      ]
    }
  }
}
```

## Альтернативные способы

Вместо использования этого скрипта, вы также можете:
1. Использовать команду VSCode: `Repomix Refresh Bundles` (если установлено расширение Repomix Runner)

- У меня почему то не работает эта опция, так и появился инструмент в этом репозитории =)

2. Запускать repomix вручную для каждого бандла

## Дополнительные ресурсы

📚 **Официальная документация Repomix**:
- [Руководство по началу работы](https://repomix.com/guide/)
- [Командная строка и опции](https://repomix.com/guide/command-line-options)
- [Использование Repomix](https://repomix.com/guide/usage)
- [Настройка MCP Server](https://repomix.com/guide/mcp-server)

💡 **Сообщество и поддержка Repomix**:
- [Discord сообщество Repomix](https://repomix.com/) - для получения помощи и обсуждения
- [GitHub репозиторий Repomix](https://github.com/repomix/repomix) - для сообщений об ошибках и предложений