# Repomix Bundle Generator Docker Image

Docker-образ для автоматической генерации бандлов [Repomix](https://repomix.com/) в формате Markdown без необходимости установки repomix в систему.

📖 **Официальная документация Repomix**: [https://repomix.com/guide/](https://repomix.com/guide/)

## Описание

Этот Docker-образ позволяет автоматически генерировать все бандлы, определенные в конфигурационном файле `.repomix/bundles.json`, создавая для каждого бандла отдельный файл в формате Markdown.

При запуске контейнера автоматически выполняется скрипт `repomix-regenerate_bundles.py`, который:
- Читает конфигурацию из `.repomix/bundles.json`
- Для каждого бандла проверяет существование файлов/директорий
- Запускает `repomix --stdin --style markdown` для генерации выходного файла
- Создает файлы вида `repomix-<имя_бандла>-output.md`

## Требования

- **Docker** - для запуска в вашей ОС

## Быстрый старт

### Использование опубликованного образа (рекомендуется)

```bash
docker run --rm \
    -v "$(pwd)":/workspace \
    -w /workspace \
    medphisiker/repomix-bundle-generator:latest
```

### Сборка образа (для желающих собрать самостоятельно)

```bash
cd utils/repomix-bundle-generator
docker build -t repomix-bundle-generator .
```

### Запуск после сборки

```bash
docker run --rm \
    -v "$(pwd)":/workspace \
    -w /workspace \
    repomix-bundle-generator
```

## Результаты работы

После успешного выполнения будут сгенерированы файлы в формате Markdown:
- `repomix-project-output.md` - описание проекта
- `repomix-solution-output.md` - основное решение
- `repomix-utils-output.md` - утилиты и вспомогательные модули
- `repomix-docs-output.md` - документация
- `repomix-plan-output.md` - планы и стратегии

### Пример вывода при запуске

```
Найдено бандлов: 6
Генерация бандла project...
Сгенерирован файл: repomix-project-output.md
Генерация бандла solution...
Сгенерирован файл: repomix-solution-output.md
Генерация бандла utils...
Сгенерирован файл: repomix-utils-output.md
Генерация бандла docs...
Сгенерирован файл: repomix-docs-output.md
Генерация бандла plan...
Сгенерирован файл: repomix-plan-output.md
Генерация бандлов завершена
```

## Возможные предупреждения

- **Файл не найден**: Если в конфигурации указан файл, который не существует, скрипт выведет предупреждение, но продолжит работу
- **Пустой бандл**: Если для бандла не найдено ни одного существующего файла, генерация пропускается

Все бандлы генерируются в формате Markdown и готовы к использованию в веб-чатах с нейросетями.

## Пример конфигурации

Скрипт работает с конфигурацией вида:
```json
{
  "bundles": {
    "project-722": {
      "name": "project",
      "files": [
        "description.md",
        "pyproject.toml",
        "README.md"
      ]
    },
    "solution-484": {
      "name": "solution",
      "files": [
        "solution.py"
      ]
    }
  }
}
```

## Обработка ошибок

Скрипт обрабатывает следующие ситуации:
- Отсутствие repomix в системе
- Отсутствие файла конфигурации `.repomix/bundles.json`
- Ошибки при чтении JSON
- Отсутствие файлов/директорий, указанных в конфигурации

## Альтернативные способы

Вместо использования этого Docker-образа, вы также можете:
1. Использовать команду VSCode: `Repomix Refresh Bundles` (если установлено расширение Repomix Runner)
2. Запускать repomix вручную для каждого бандла
3. Установить repomix в систему и использовать скрипт напрямую

## Дополнительные ресурсы

📚 **Официальная документация Repomix**:
- [Руководство по началу работы](https://repomix.com/guide/)
- [Командная строка и опции](https://repomix.com/guide/command-line-options)
- [Использование Repomix](https://repomix.com/guide/usage)

💡 **Сообщество и поддержка Repomix**:
- [Discord сообщество Repomix](https://repomix.com/) - для получения помощи и обсуждения
- [GitHub репозиторий Repomix](https://github.com/repomix/repomix) - для сообщений об ошибках и предложений